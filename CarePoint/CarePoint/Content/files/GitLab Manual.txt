// now in <YourBranch>, assume that you have the last updated version
git add .
git commit -m "write commit"
git checkout master

// now in master
git pull origin master
git checkout <YourBranch>

// now in <YourBranch>
git merge master

// now solve conflicts on <YourBranch> from Visual Studio then
git add .
git commit -m "write comment"
git push origin <YourBranch>

// then go to gitlab web site and choose <YourBranch> then click on "create merge request" => "save changes"